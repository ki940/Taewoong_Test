#pragma once
int RandomValue();
char* Casting(const char* str);
void printRSP(const char* name, int value);

char* RspValueCompare(int playerValue);
char* RspValueCompare2(int playerValue);

void RspGameTest();



const char* RSP(const char* name, int value);
const char* RspValueCompare3(int playerValue);
/*
솔루션에서 새 프로젝트를 만들거나 
새 디렉토리를 만들면서 파일을 옮길 때.
1. 실제 폴더 헤더랑 cpp 파일이 옮김(솔루션 탐색기 - 모든 파일 표시)
2. 솔루션 탐색기 내 에서도 파일이 옮겨져야함.
1,2번 둘다 사람이 직접 해야 파일이 옮겨짐
12번 이후 탐색기 가서 추가 > 기존 항목 > 옮긴 파일

*/


//void RspGameTest2();